rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
    // v3.41: 경쟁사 가격 컬렉션 명시적 허용
    match /competitors/{id} {
      allow read, write: if true;
    }
  }
}
